@model IEnumerable<Employment.Frontend.Models.State>;
@{
    ViewData["Title"] = "Index";
    int idno = 1;
}
<div class="container border mt-5 p-3">
    <p class="text-center">
        <a class="btn btn-primary" asp-action="AddorEdit"><i class="fa-solid fa-plus"></i> Create New</a>
    </p>

    <table class="table" id="stateTable">
        <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">Country Name</th>
                <th scope="col">State Name</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            @{
                foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @idno
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem=>item.Country.CountryName)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem=>item.StateName)
                        </td>
                        <td class"text-end">
                                    <a href="Country/AddorEdit/@item.Id" class="btn btn-success">Edit</a>
                            <a class="delete-button btn btn-danger" href="State/Delete/@item.Id" onclick="return confirm('Are you sure you want to delete this item?');"><i class="fa-solid fa-trash"></i> Delete</a>
                        </td>
                    </tr>
                    idno++;
                }
            }
        </tbody>
    </table>
</div>

